$("#post_<%= @post.id %> .comments-count").html("コメント数：<%= @post.comments.count %>");
$(".comments").html("<%= j(render 'public/comments/comment', post: @post, comment: @comment) %>");
$("#comment_comment").val("");