<div class="d-flex mb-3">
  <p class="mr-5">本文 :</p>
  <p><%= post.body %></p>
</div>
<div class="d-flex">
  <p class="mr-5">タグ :</p>
  <% post.tags.each do |tag| %>
    <p class="tag mr-4"><%= tag.name %></p>
  <% end %>
</div>